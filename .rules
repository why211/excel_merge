1. 安装必要的库：`pandas` 和 `openpyxl`。
2. 编写一个 Python 脚本，遍历指定文件夹中的所有 Excel 文件（.xlsx）。
3. 对每个 Excel 文件，读取其中的“学号”和“*学生姓名”两列的数据。
4. 将所有文件中读取到的数据追加到一个总的 DataFrame 中。
5. 将最终合并后的 DataFrame 保存为一个新的 Excel 文件（如 result.xlsx）。

注意事项：
- 文件夹路径和输出文件名请用变量表示，方便修改。
- 需要考虑部分文件可能缺少这两列的情况，遇到缺失时跳过该文件并给出提示。
- 输出的 Excel 文件需包含“学号”和“*学生姓名”两列。

请直接给出完整的 Python 代码实现，谢谢！

1.输出的结果同步到excel表中可以选择是否去重。
2.首先确定一共有多少个字段。
3.然后根据输入的字段来确定需要合并的excel表。