1. 安装必要的库：`pandas` 和 `openpyxl`。
2. 编写一个 Python 脚本，遍历指定文件夹中的所有 Excel 文件（.xlsx）。
3. 对每个 Excel 文件，读取其中的“学号”和“*学生姓名”两列的数据。
4. 将所有文件中读取到的数据追加到一个总的 DataFrame 中。
5. 将最终合并后的 DataFrame 保存为一个新的 Excel 文件（如 result.xlsx）。

注意事项：
- 文件夹路径和输出文件名请用变量表示，方便修改。
- 需要考虑部分文件可能缺少这两列的情况，遇到缺失时跳过该文件并给出提示。
- 输出的 Excel 文件需包含“学号”和“*学生姓名”两列。

请直接给出完整的 Python 代码实现，谢谢！

1.首先我不需要分析字段，只需要处理文件即可。
2.我可以选择多个字段进行导入到目的excel。
3.首先选择是否可以去重，如果去重，则需要选择去重的字段。
4.另外可以生成一个执行函数路线图，帮助我来更好理解。

git rm -r --cached test_functions.py
git commit -m "从Git中移除excel文件夹"
git push origin main

1.excel中有个字段为学号，但是该excel中没有学生姓名。
2.通过选择所有的excel，从excel中没有学生姓名的，去其他所有有的excel拿取学生姓名。
3.如果其他excel，通过学号查看其他已经被选择excel学生姓名没有查看到，则给一个指定的默认值。
你无需处理上述问题，将上面问题的处理要求在代码框中生成提示词，我要交给agent替我处理问题